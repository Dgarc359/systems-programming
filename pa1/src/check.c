#include "balance.h"

int check(char str[]) {
  // char c = opening_symbols[0];
  // char c2 = opening_symbols[0];
  printf("open: %c close: %c", opening_symbols[0], closing_symbols[0]);

  int length = 0;
  int check;
  // for(int i = 0; check != NULL; check = opening_symbols_pointer[i], i++, length++);

  if(length == 0) {
    // use parenthesis to check balance
  } else {
    // length is not 0 so user has defined their own pairs
  }
  // returns 1 if input stream contains a balanced string
  // 0 otherwise

  // use most recent list of pairs
  // use a stack to check the balance of opening and closing symbols

  return 1;
}